CREATE OR REPLACE VIEW MEDIA
AS SELECT D.DEPT_NO, DNOMBRE, AVG(SALARIO), MAX(SALARIO) 
FROM DEPART D, EMPLE E
WHERE E.DEPT_NO=D.DEPT_NO
GROUP BY D.DEPT_NO

INSERT INTO MEDIA VALUES(50, 'MARKETING', 2000, 2500)
UPDATE MEDIA SET DEPT_NO = 60 WHERE DEPT_NO = 50
DELETE FROM MEDIA WHERE DEPT_NO = 60